spring.application.name=airportPro

# Configuración de R2DBC para PostgreSQL
spring.r2dbc.url=${DB_URL}
spring.r2dbc.username=${DB_USER}
spring.r2dbc.password=${DB_PASS}
application.security.jwt.secret-key=${JWT_SECRET_KEY}
application.security.jwt.expiration=${TOKEN_TIME}
application.security.jwt.refresh-token.expiration=${REFRESH_TOKEN_TIME}

# Configuración de la inicialización de la base de datos
#spring.sql.init.mode=always
spring.main.allow-bean-definition-overriding=true
# Configuración del DataSource para el driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver
